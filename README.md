# Demo of spring-boot application using Liquibase to manage Cassandra updates
This application runs locally.
Before running this application Make sure cassandra is running on your machine and the keyspace exists.
Run application using the command below:
```bash
mvn spring-boot:run -Plocal-liquibase
```
